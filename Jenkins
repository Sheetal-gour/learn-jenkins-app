pipeline {
    agent any

    stages {
         stage('Cleanup') {\
                cleanWs();         
         }
         stage('Build') {
            agent {
                docker {
                    image 'node:22-alpine'
                    reuseNode true
                }
            }
            steps {

                echo 'Hello World from docker container'
                sh '''
                ls -ltr
                npm install
                npm run build
                '''
            }
        }
    }
}
